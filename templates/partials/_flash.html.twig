{% for type, messages in app.flashes %}
    <div class="position-fixed top-0 end-0 p-3 agf-toast-zone">
        {% for message in messages %}
            <div class="alert alert-{{ type }} shadow-sm mb-2" role="alert">
                {{ message }}
            </div>
        {% endfor %}
    </div>
{% endfor %}
